# Frontend Assets

Здесь лежат стили и JS-модули frontend.

Структура:

- `app.css` — визуальная система и layout;
- `api.js` — тонкий HTTP-клиент;
- `state.js` — локальное состояние UI;
- `render.js` — рендеринг секций;
- `app.js` — orchestration UI-событий.
